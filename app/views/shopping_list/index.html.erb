<div> Shopping List </div>

<div> Amount of foods that you have to buy <%= @final.length %> </div>

<div> Total value of food needed <%= @total %> </div>

<table>
    <tr>
        <th>Food</th>
        <th>Quantity</th>
        <th>Price</th>
    </tr>

    <% @final.each do |food| %>
        <tr>
            <td> <%= food[:name] %> </td>
            <td> <%= food[:quantity] %> </td>
            <td> <%= food[:price] %> </td>
        </tr>
    <% end %>
</table>
